"""Shared defaults for the standalone on-Pi capture/inference/display pipeline."""

DEFAULT_WIDTH = 640
DEFAULT_HEIGHT = 480
DEFAULT_CAPTURE_FPS = 20   # hard cap on the capture/infer/display loop rate
